# MAGI UI-D4b-2d 交付摘要

创建决策和运行决策现在支持显式异步模式。客户端发送
`Prefer: respond-async` 后立即获得可持久化查询的任务回执；未发送时继续使用
原有同步行为。

新增任务状态与事件回放接口，并落实所有者遮蔽、决策权限复核、私有缓存头、
稳定错误响应和 OpenAPI 声明。生产启动流程会同时启动后台工作器，关闭时安全
停止，未完成任务可依靠租约恢复。

下一增量为 UI-D4b-2e：将异步创建、运行、断线恢复和阶段进度接入 EVA 风格
Web 工作区。
