# MAGI M5b 验收摘要

版本：`0.5.0a2`

M5b 将单次离线评估接入生产数据链。服务端从已验证审计链重建决策终态，从模型
调用账本读取延迟和令牌数据，客户端只能指定决策 ID 与版本，不能上传投票、
裁决或指标结果。

评估历史使用 PostgreSQL 追加式表、事务级 advisory lock 和禁止更新/删除触发器。
相同完整评估摘要自动返回原记录，不会因重试扩大历史。读取与运行分别要求
`evaluation:read`、`evaluation:run`，API 和 CLI 输出相同的历史及趋势语义。
