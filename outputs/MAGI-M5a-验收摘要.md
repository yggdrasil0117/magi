# MAGI M5a 验收摘要

版本：`0.5.0a1`

M5a 已建立不参与投票、不调用模型的确定性评估层。统一评估包覆盖引用有效性、
三人格差异度、裁决一致性、延迟和显式费率成本五项指标，并可通过 `magi-eval`
输出稳定 JSON、作为 CI 阈值门禁。

关键安全约束：所有输入记录必须属于同一决策版本；未提供调用样本或完整费率时
显示 `not_measured`，不会伪装为通过；缓存复用不重复计算供应商延迟和费用；
仓库不写死任何实时模型价格。
