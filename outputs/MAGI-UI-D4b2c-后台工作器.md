# MAGI UI-D4b-2c 交付摘要

后台工作器已完成：任务可从 PostgreSQL 原子领取，在执行期间续租，并通过
fencing token 阻止失效工作器覆盖新结果。进程中断时任务不会被误判失败，租约
过期后可由其他工作器恢复。

创建与运行均复用现有应用服务和 LangGraph 状态，不另建决策逻辑。公开事件只
保存阶段和稳定消息码，不写入提示词、人格推理或内部异常文本。

下一增量是 UI-D4b-2d：提供异步提交、状态/结果查询与事件回放 API。
